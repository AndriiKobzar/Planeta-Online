@model Planeta_Online.Models.VisitorsViewModel
@{
    ViewBag.Title = "Visitors";
}

<h2>Заявки на @Model.EventName</h2>
<table>
   @for(int i=0; i<Model.Visitors.Count; i++)
    {
        <tr>
            <td>@(i+1))</td>
            <td>@Model.Visitors[i]</td>
        </tr>
    }
</table>